
  import "./example.openapi";

  import express, { type Router } from "express";

export const example: Router = express.Router();

// TODO: Add your routes here
// Example:
// example.get("/", handler);
// example.post("/", validateBody(schema), handler);
